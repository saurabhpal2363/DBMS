Questions that practice in the classroom
It's Queries were important for Exam purpose.

1. JOINS
2. CASCADING
3. GROUP BY
4. HAVING
5. CHECK
6. CLAUSES
7. VIEW

CREATE DATABASE NewDatabase;
USE NewDatabase;

CREATE TABLE Student(
	Sno INT PRIMARY KEY,
    Name VARCHAR(50),
    RollNo INT UNIQUE,
    Age INT,
    Course VARCHAR(15)
);

INSERT INTO Student
VALUES
(1, 'Saurabh', 240, 20, 'Btech CSE'),
(2, 'Adarsh', 261, 25, 'Btech CSE'),
(3, 'Adi', 541, 65, 'Btech CSE'),
(4, 'Abhinav', 658, 45, 'Btech CSE'),
(5, 'Ashish', 684, 78, 'Btech CSE'),
(6, 'Sujal', 845, 15, 'Btech AIML'),
(7, 'Siraj', 458, 47, 'Btech Dept.')

SELECT * FROM Student
ORDER BY RollNo ASC;

Ques. Find the name of all students with even rollNo
SELECT name
FROM Student
WHERE RollNo IN (
	SELECT RollNo
	FROM Student
	WHERE RollNo % 2 = 0
);

Ques. Find the max marks of the student that belongs to DELHI
SELECT MAX(marks)
FROM (
	SELECT * FROM Student
	WHERE city = "Delhi"
) AS temp;

# MySQL Views According to teacher
CREATE VIEW view1 AS
SELECT RollNo, name, marks 
FROM Student;

SELECT * FROM view1;

DROP VIEW view1;
