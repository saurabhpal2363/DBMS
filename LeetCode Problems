// Ques based on Ranking of score
Ques. Problen No. 178
SELECT score,
DENSE_RANK() OVER (ORDER BY score DESC)
AS 'rank' FROM Scores;

// Ques Based on Teacher table
Ques. Problen No. 2356
SELECT 
    teacher_id,
    COUNT(DISTINCT subject_id) AS cnt
FROM Teacher
GROUP BY teacher_id;
